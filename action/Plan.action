###### The motion planning interface

### Action Server Goal

# The goal to be reached at the end of the plan.
Location goal

---

### Action Server Result

# The plan, i.e., the `via_points` to reach the `goal` given the current position.
Location[] via_points

---

### Action Server Feedback

# The set of `via_points` computed so far.
Location[] via_points
